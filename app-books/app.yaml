kind: Service
apiVersion: v1
metadata:
  name: app-books
  labels:
    app: app-books
spec:
  type: ClusterIP
  selector:
    app: app-books
  ports:
    - name: tcp
      port: 8080
      protocol: TCP
      targetPort: 8080
---
kind: Deployment
apiVersion: apps/v1
metadata:
  name: app-books
spec:
  replicas: 1
  selector:
    matchLabels:
      app: app-books
  template:
    metadata:
      labels:
        app: app-books
        version: v1
    spec:
      containers:
      - name: app-books
        image: app-books
        imagePullPolicy: IfNotPresent
        ports:
        - containerPort: 8080
