apiVersion: v1
kind: ConfigMap
metadata:
  name: consul-config
  namespace: book-store
data:
  consul-config.hcl: |
    server = true
    ui_config {
      enabled = true
    }
    bootstrap_expect = 1
    client_addr = "0.0.0.0"
    bind_addr = "0.0.0.0"
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: consul
  namespace: book-store
  labels:
    app: consul
spec:
  replicas: 1
  selector:
    matchLabels:
      app: consul
  template:
    metadata:
      labels:
        app: consul
    spec:
      containers:
      - name: consul
        image: consul:1.15.4
        imagePullPolicy: IfNotPresent
        ports:
        - containerPort: 8500
          name: http
          protocol: TCP
        - containerPort: 8600
          name: dns
          protocol: UDP
        env:
        - name: CONSUL_BIND_INTERFACE
          value: eth0
        resources:
          requests:
            memory: "256Mi"
            cpu: "250m"
          limits:
            memory: "512Mi"
            cpu: "500m"
        command:
        - consul
        - agent
        - -server
        - -ui
        - -bootstrap-expect=1
        - -client=0.0.0.0
        - -bind=0.0.0.0
        livenessProbe:
          exec:
            command:
            - consul
            - members
          initialDelaySeconds: 30
          periodSeconds: 10
          timeoutSeconds: 5
          failureThreshold: 3
        readinessProbe:
          exec:
            command:
            - consul
            - members
          initialDelaySeconds: 10
          periodSeconds: 10
          timeoutSeconds: 5
          failureThreshold: 3
        volumeMounts:
        - name: consul-data
          mountPath: /consul/data
      volumes:
      - name: consul-data
        persistentVolumeClaim:
          claimName: consul-pvc
---
apiVersion: v1
kind: Service
metadata:
  name: consul
  namespace: book-store
  labels:
    app: consul
spec:
  type: ClusterIP
  ports:
  - port: 8500
    targetPort: 8500
    protocol: TCP
    name: http
  - port: 8600
    targetPort: 8600
    protocol: UDP
    name: dns
  selector:
    app: consul
---
apiVersion: v1
kind: Service
metadata:
  name: consul-ui
  namespace: book-store
  labels:
    app: consul
spec:
  type: ClusterIP
  ports:
  - port: 8500
    targetPort: 8500
    protocol: TCP
    name: http
  selector:
    app: consul
---
apiVersion: route.openshift.io/v1
kind: Route
metadata:
  name: consul-ui
  namespace: book-store
  labels:
    app: consul
spec:
  host: consul.apps.openshift.example.com
  to:
    kind: Service
    name: consul-ui
    weight: 100
  port:
    targetPort: http
